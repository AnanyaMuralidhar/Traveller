
<%= stylesheet_link_tag 'login' %>

<nav class="navbar navbar-expand-lg" style="background-color:	#004d99;" data-bs-theme="dark">
  <div class="container-fluid">
      <%= link_to image_tag('suitcase_logo.png',size: "30x30"), class:"d-inline-block align-text-top", width:"30" ,height:"24" %>
      <!-- <img src="#" width="30" height="24" class="d-inline-block align-text-top"> -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to 'Traveller', root_path, class:'nav-link' %>
        </li>
        <li class="nav-item">
          <%= link_to 'About Us', home_about_path, class:'nav-link' %>
        </li>
      </ul>
    </div>
  </div>
</nav>

<div class="signup">
  <div class="signup-connect">
    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <h1><center>Log In</center></h1>
    <a href="#" class="btn btn-social btn-facebook"><i class="fa fa-facebook"></i> Sign in with Facebook</a>
    <a href="#" class="btn btn-social btn-twitter"><i class="fa fa-twitter"></i> Sign in with Twitter</a>
    <a href="#" class="btn btn-social btn-google"><i class="fa fa-google"></i> Sign in with Google</a>
    <a href="#" class="btn btn-social btn-linkedin"><i class="fa fa-linkedin"></i> Sign in with Linkedin</a>
  </div>
  <div class="signup-classic">
    <h2>Or use the classical way</h2>
    <form class="form">
      <fieldset class="username">
        <%= f.label :email %><br />
        <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
      </fieldset>
      <fieldset class="email">
        <%= f.label :password %><br />
        <%= f.password_field :password, autocomplete: "current-password" %>
      </fieldset>

          <%= f.submit "Log in" ,class:"btn btn-primary" %>
    </form>
  </div>
</div>
<% end %>
